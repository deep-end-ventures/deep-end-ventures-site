<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 240 240" fill="none">
  <!-- Deep End Ventures: Abstract depth mark - nested chevrons diving down -->
  <defs>
    <linearGradient id="dev-grad" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#1e40af"/>
      <stop offset="100%" stop-color="#0a1628"/>
    </linearGradient>
  </defs>
  <!-- Outer chevron -->
  <path d="M120 200 L40 100 L80 100 L120 155 L160 100 L200 100 Z" fill="url(#dev-grad)" opacity="0.4"/>
  <!-- Middle chevron -->
  <path d="M120 170 L55 85 L90 85 L120 125 L150 85 L185 85 Z" fill="url(#dev-grad)" opacity="0.7"/>
  <!-- Inner chevron (boldest) -->
  <path d="M120 140 L70 70 L100 70 L120 97 L140 70 L170 70 Z" fill="url(#dev-grad)"/>
  <!-- Top accent bar -->
  <rect x="70" y="45" width="100" height="6" rx="3" fill="#1e40af"/>
</svg>
